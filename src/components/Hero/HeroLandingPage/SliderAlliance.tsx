import React from 'react';

interface SliderAllianceProps {
    // Define the props for your component here
}

const SliderAlliance: React.FC<SliderAllianceProps> = (props) => {
    return (
        <div>

        </div>
    );
};

export default SliderAlliance;